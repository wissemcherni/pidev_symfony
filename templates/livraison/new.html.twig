{% extends 'baseback.html.twig' %}

{% block title %}New Livraison{% endblock %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-warning text-white">
                        <h3 class="card-title">Create new Livraison</h3>
                    </div>
                    <div class="card-body">
                        {{ include('livraison/_form.html.twig') }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_livraison_index') }}" class="btn btn-warning">back to list</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

